// const db = require('../db/index');

// class BusController {
//     async addBuses(req, res) {
//         const queries = 'INSERT INTO buses (number_plate, manufacturer, model, year, capacity) VALUES ($1, $2, $3, $4)';
//         const { number_plate, manufactuor}
//     }
// }
